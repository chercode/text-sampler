version: '3.8'

services:
  server:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app/src  # Hot reload: changes in 'src' update the container immediately
    environment:
      - PYTHONUNBUFFERED=1